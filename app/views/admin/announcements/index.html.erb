<h1>お知らせ一覧</h1>

<%= link_to "新規作成", new_admin_announcement_path %>

<table>
  <tr>
    <th>タイトル</th>
    <th>操作</th>
  </tr>
  <% @announcements.each do |a| %>
    <tr>
      <td><%= a.title %></td>
      <td>
        <%= link_to "編集", edit_admin_announcement_path(a) %> 
      </td>
    </tr>
  <% end %>
</table>
